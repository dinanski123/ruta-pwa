{
  "name": "ruta-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor-web.mjs",
    "cap:add:android": "npm run cap:prepare && npx cap add android",
    "cap:sync:android": "npm run cap:prepare && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1"
  }
}
